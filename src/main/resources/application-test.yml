spring:
  autoconfigure:
    exclude:
      - org.springframework.ai.vectorstore.milvus.autoconfigure.MilvusVectorStoreAutoConfiguration

  ai:
    # Override DashScope with the Spring AI test mock during tests
    dashscope:
      api-key: test-key

    vectorstore:
      milvus:
        client:
          host: ""
